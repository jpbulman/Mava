package Src;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class Mava {

//    private static int addOne(int i){
//
//        int length = 1;
//
//        while(i % Math.pow(10,length) != i){
//            length++;
//        }
//
//        int toModify = i;
//        for(int power = 1;power <= length;power++){
//            int divisor = (int) Math.pow(10,power-1) == 0 ? 1:(int) Math.pow(10,power-1);
//            int currentDigit = (int) ((i % Math.pow(10,power)) - (i % Math.pow(10,power - 1))) / divisor;
//
//            if(currentDigit != 9){
//                toModify += (int) Math.pow(10,power-1);
//            } else {
//                int rightPart = toModify % (int) Math.pow(10,power);
//                int leftPart = toModify - (rightPart);
//                leftPart += Math.pow(10,power-1);
//                leftPart *= 10;
//
//                rightPart = rightPart % (int) Math.pow(10,power-1);
//
//                toModify = leftPart+rightPart;
//                power++;
//                length++;
//            }
//        }
//
//        return toModify;
//    }

//    public static void tester(){
//        assertEquals(Src.RealNumberOperations.sum(3,2), 5.0);
//    }

    //mersenne primes
    //is a power of
    //is prime
    //lexographic permutations
    //fibonacci sets
    //lucas numbers
    //nth factorial
    //triangle numbers
    //product of numbers

    public static void main(String[] args) {
//        tester();
    }

}
